<!--
  1.需要给组件提供一个父元素并指定高度,组件高度由父元素控制。否则将出现问题
  2.如果需要下拉刷新 ，请不要使用本组件

  参数说明:
  data [Object] : 列表所需要的数据
  my-city [String]: 是否开启我的定位 ，默认为false ，传入城市名称。不写或者空，为关闭

  -->
<view wx:if="{{is_on_audite}}">
  <view class="weui-panel weui-panel_access">
      <view class="weui-panel__bd">
          <block wx:for="{{auditeData}}" wx:key="{{id}}">
              <navigator url="../projectDetails/projectDetails" class="weui-media-box weui-media-box_appmsg" hover-class="weui-cell_active"> 
          <view class="weui-media-box__hd weui-media-box__hd_in-appmsg">
            <image class="weui-media-box__thumb" src='{{item.imgSrc}}' />
          </view>
          <view class="weui-media-box__bd weui-media-box__bd_in-appmsg">
            <view class="weui-media-box__title"> {{item.title}}</view>
            <view class="weui-media-box__desc">{{item.description}}</view>
          </view>
         </navigator>
          </block>
      </view>
    </view> 
</view>
<view wx:else>
  <view wx:if='{{projectListData.length > 0}}' class='wrapper'>
    <list-html data="{{projectListData}}" bindDownRefresh="onPullDownRefresh" binddetail="bindtap"></list-html>
  </view>

</view>