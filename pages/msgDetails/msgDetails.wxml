<!--pages/msgDetails/msgDetails.wxml-->
<view class='warpper'>
  <block wx:for="{{array}}" wx:key="{{id}}">
    <view class='c-Box'>
       {{item.content}} 
    </view>
  </block>
  <view wx:if="{{!array.length}}" class='none'>--您没有通知公告--</view>
</view>